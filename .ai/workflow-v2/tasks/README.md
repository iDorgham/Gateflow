# Task contracts

Ad-hoc `/dev loop` work requires a versioned task contract containing target,
included/excluded paths, acceptance criteria, security boundaries, checks,
rollback, and prohibited actions. `/dev loop task approve` hashes the complete
contract; any later edit invalidates approval.
