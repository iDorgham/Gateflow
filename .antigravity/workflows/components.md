---
name: components
description: Map page sections to reusable components, variants, and states.
---

# /components [route]

Search `@gateflow/ui`, `@gateflow/components`, and app components first. Record
page → section → component → variant → state, consumers, tokens, and a
reuse/extract/consolidate/new decision. Reject nested-card systems and competing
primitives. Read-only except workspace documentation.
