{
  "name": "/ralph",
  "version": "1.0.0",
  "description": "Recursive autopilot: implement ALL remaining phases of the active plan end-to-end until complete. Includes session memory, TDD, enforcement, verification, PR.",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "Read and follow the GateFlow /ralph workflow exactly as defined in .agents/workflows/ralph.md\n\nLoad it now and execute it. User input: {{args}}"
  }
}
