{
  "name": "/version",
  "version": "1.0.0",
  "description": "Semantic versioning \u2014 bump package.json, create annotated git tags, generate versioned branch names.",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "Read and follow the GateFlow /version workflow exactly as defined in .agents/workflows/version.md\n\nLoad it now and execute it. User input: {{args}}"
  }
}
