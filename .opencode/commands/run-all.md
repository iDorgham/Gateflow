---
description: Run all development servers in parallel
agent: build
---

Start all development servers in the monorepo using turbo. Run: pnpm turbo dev

If there are issues, report which apps failed to start.
