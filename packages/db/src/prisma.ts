/**
 * Prisma-generated types and enums only. No DB client, seed helpers, or Node-only imports.
 * Use from client components instead of `@gate-access/db` to avoid pulling the full package barrel into the browser bundle.
 */
export * from '@prisma/client';
