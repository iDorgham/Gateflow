description = "Docs folder cleanup — scan structure, remove empty dirs and dead symlinks, rebuild docs/INDEX.md."
prompt = """
Follow the workflow defined in the organize workflow to Keep the docs/ folder clean, structured, and navigable.

@{.agents/workflows/organize.md}

User input: {{args}}
"""
