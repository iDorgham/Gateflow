# Prettier refuses to format symlink targets when paths are passed explicitly (lint-staged).
docs/CLAUDE.md
docs/PRD.md

# Partytown lib is generated by `pnpm partytown:copy` — do not reformat.
apps/marketing/public/~partytown/**

# pnpm lockfile must stay in pnpm's own YAML layout.
pnpm-lock.yaml
