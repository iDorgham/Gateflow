# Backlog

Raw tasks and ideas. Use **`/man tasks add "Title"`** to quick-add. Refine into IDEA + plan via `/idea` and `/plan`.

- **ALL_TASKS_BACKLOG.md** — Main backlog (create when needed; or use `docs/archive/plan-legacy/backlog/ALL_TASKS_BACKLOG.md` as reference).
- **quick-*.md** — Quick-capture files by date.
