# API notes — resident_portal_responsive

- Document any **new or changed** routes or server actions under `apps/resident-portal/` (auth first, Zod, `organizationId` scope).
- Link to handler files and list query params / body shapes after implementation.

_Populate during Phase 2+ (PWA / push) and later phases as needed._
