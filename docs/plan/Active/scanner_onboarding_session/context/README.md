# Plan context — `scanner_onboarding_session`

Use this folder for **plan-local** references so `/dev` does not rely on chat memory.

| File               | Purpose                                           |
| ------------------ | ------------------------------------------------- |
| `api.md`           | Scanner shift / onboarding API routes             |
| `contracts.md`     | Security / multi-tenancy reminders and links      |
| `database.md`      | `ShiftLog`, migrations, seed notes                |
| `design.md`        | Onboarding wizard, ADS tokens, home screen layout |
| `structure.md`     | File map under `apps/scanner-app`                 |
| `documentation.md` | PRD paths, mobile guides, i18n conventions        |

**Global learning:** `docs/development/learning/` — cross-plan patterns.  
**Per-phase issues:** `../phase_logs/PHASE_LOG_phase_NN.md` (mandatory after each `/dev` phase).
