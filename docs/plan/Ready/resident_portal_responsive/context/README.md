# Plan context — `resident_portal_responsive`

Use this folder for **plan-local** references so `/dev` does not rely on chat memory.

| File               | Purpose                                                   |
| ------------------ | --------------------------------------------------------- |
| `api.md`           | Resident-portal API routes or server actions touched      |
| `contracts.md`     | Security / multi-tenancy reminders and links              |
| `database.md`      | Prisma models, migrations if portal touches shared schema |
| `design.md`        | Layout shells, breakpoints, ADS / PWA notes               |
| `structure.md`     | File map under `apps/resident-portal`                     |
| `documentation.md` | PRD paths, guides, i18n key conventions                   |

**Global learning:** `docs/development/learning/` — cross-plan patterns.  
**Per-phase issues:** `../phase_logs/PHASE_LOG_phase_NN.md` (mandatory after each `/dev` phase).
