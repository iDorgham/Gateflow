# Design notes — resident_portal_responsive

- `useBreakpoint`, `PortalShell`, `BottomNav` / `Sidebar`, PWA manifest and offline QR UX.
- RTL: logical properties (`ms-*` / `me-*`) on new layout surfaces.

_Populate with Figma links or ADS token notes as needed._
