# API notes — scanner_onboarding_session

- Document **scanner** shift and onboarding endpoints (auth, Zod, `organizationId` on all tenant data).
- Link to handler files under the appropriate app or API package after implementation.

_Populate during Phase 3+._
