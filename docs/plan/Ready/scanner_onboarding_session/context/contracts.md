# Contracts — scanner_onboarding_session

- Mobile: tokens in **SecureStore** only (not AsyncStorage for auth).
- `ShiftLog` and scan paths: explicit `organizationId` scope; soft deletes where models support it.

_Populate with phase-specific callouts as work lands._
