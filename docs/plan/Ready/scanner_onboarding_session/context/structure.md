# Structure — scanner_onboarding_session

- **App:** `apps/scanner-app/src/` — screens, hooks, security utilities.
- Prefer shared types from `@gate-access/types` when crossing API boundaries.

_Populate with navigator and context file map during /dev._
