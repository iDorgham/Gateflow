# Contracts — gateflow_design_system

- **Monorepo:** `pnpm` only; `workspace:*` until publish.
- **Publishing:** semver, Changesets, `NPM_TOKEN` in CI (Phase 10).
- **Security:** No secrets in docs app or Storybook-like surfaces.
