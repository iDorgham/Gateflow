# Plan context — `gateflow_design_system`

| File               | Purpose                                               |
| ------------------ | ----------------------------------------------------- |
| `api.md`           | Any API routes or metadata endpoints for the docs app |
| `contracts.md`     | Publishing, semver, workspace protocol constraints    |
| `database.md`      | N/A unless a phase adds persistence                   |
| `design.md`        | Token ramps, motion policy, IA links                  |
| `structure.md`     | Monorepo package boundaries                           |
| `documentation.md` | External sites (`design.gateflow.site`, npm)          |

**Per-phase issues:** `../phase_logs/PHASE_LOG_phase_NN.md` (mandatory after each `/dev` phase).
