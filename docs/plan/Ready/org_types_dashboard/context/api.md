# API notes — org_types_dashboard

- Document any **new or changed** routes under `apps/client-dashboard/src/app/api/` (auth first, Zod, `organizationId` scope).
- Link to handler files and list query params / body shapes after implementation.

_Populate during Phase 1+._
