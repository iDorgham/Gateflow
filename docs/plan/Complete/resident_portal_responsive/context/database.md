# Database notes — resident_portal_responsive

- Portal mostly consumes existing APIs; note any **shared** Prisma changes here (model, migration name).

_Populate if resident-portal introduces or extends schema._
