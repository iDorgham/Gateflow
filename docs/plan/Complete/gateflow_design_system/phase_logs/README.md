# Phase logs — `gateflow_design_system`

After each phase, add **`PHASE_LOG_phase_NN.md`** (01–10).

Capture build/turbo failures, Tailwind v3 vs v4 friction, motion dependency decisions, and npm publish blockers.

Create the file even if smooth (“No incidents.”).
