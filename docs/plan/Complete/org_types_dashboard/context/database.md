# Database — org_types_dashboard

- **Model:** `Organization` — add `type OrganizationType @default(REAL_ESTATE)` (Phase 1).
- **Enum:** `REAL_ESTATE`, `SCHOOL`, `CLUB`, `NIGHTCLUB`, `EVENT_ORGANISER`.
- **Migrations:** name clearly (e.g. `add_organization_type`).
- **Seeds:** at least one org per type for QA; align with `docs/development/initiatives/IDEA_advanced_seeding_v2.md` where applicable.
