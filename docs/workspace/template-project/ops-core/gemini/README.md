# Gemini CLI Source

Contains TOML command definitions synced to `.gemini/commands/`.

## Structure

```
gemini/
└── commands/        # .toml files — one per workspace command
```

Run `pnpm ai:sync` to propagate to `.gemini/`.
