# Gemini Commands

Place `.toml` command definition files here. They will be synced to `.gemini/commands/` by `pnpm ai:sync`.

## Format

```toml
# my-command.toml
name = "my-command"
description = "What this command does"
prompt = """
Your full prompt here.
"""
```
