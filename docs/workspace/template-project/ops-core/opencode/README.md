# OpenCode CLI Source

Contains agents and commands synced to `.opencode/`.

## Structure

```
opencode/
├── agents/          # Agent markdown files
└── commands/        # Command markdown files
```

Run `pnpm ai:sync` to propagate to `.opencode/`.
